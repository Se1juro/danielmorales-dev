name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: Echo "Hello world"
        run: |
          echo "Hello world!"
      - name: Print date
        run: |
          date
      - name: Print files
        run: |
          ls -l
